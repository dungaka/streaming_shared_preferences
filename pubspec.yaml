name: streaming_shared_preferences
description: A new flutter plugin project.
version: 0.0.2 

environment:
  sdk: ">=2.0.0 <3.0.0" 

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
 
flutter: 
  plugin:
    platforms:
      android:
        package: com.topfreelancerdeveloper.streaming_shared_preferences
        pluginClass: StreamingSharedPreferencesPlugin
      ios:
        pluginClass: StreamingSharedPreferencesPlugin
 
